from setuptools import setup

setup(
 name='evolutiveturingmachine',
 version='1.0',
 scripts=['cell.py', 'config.py', 'encode.py', 'evolutiveturingmachine.py', 'petridish.py', 'tape.py', 'gui_start', 'server_start', 'server_stop']                  # The name of your scipt, and also the command you'll be using for calling it
)
